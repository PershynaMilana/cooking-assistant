# Copy to backend/.env and fill in real values

# Secret used to sign/verify JWT auth tokens — set any long random string
JWT_SECRET_KEY=

# Database connection (read by backend/db.js)
DB_USER=postgres
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cooking_helper

# Server
PORT=8080
