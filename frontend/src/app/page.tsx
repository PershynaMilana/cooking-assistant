import type { Metadata } from "next";

export const metadata: Metadata = {
    alternates: { canonical: "/" },
};

// placeholder while the routes still live in the react-router tree
const HomePage = () => null;

export default HomePage;
