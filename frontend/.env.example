# VITE_API_URL=http://localhost:3000
# Leave unset in dev: the Vite proxy forwards /api to the backend, keeping
# requests same-origin so the httpOnly auth cookie is first-party.
# Set this only for production deployments.
